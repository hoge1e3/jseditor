native localStorage;
native process;
\new() {
    dir=localStorage.etc ? 
    FS.get(localStorage.etc) : 
    FS.get(process.cwd()).rel(".jsetc/");
}
\conf(path) {
    return new JSONConf(rel(path));
}
\rel(p) {
    return dir.rel(p);
}